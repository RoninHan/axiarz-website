# 快速开始 - 合同模板设计器测试指南

## 前提条件

- ✅ 系统已启动并运行
- ✅ 已登录管理员账号（具有 `system.manage` 权限）
- ✅ 数据库中已有至少一个测试订单

## 5分钟快速测试

### 步骤 1: 访问设计器

```
http://localhost:3000/admin/contract-template
```

**预期**: 看到三栏布局
- 左侧：11 个可拖拽组件
- 中间：空白画布（显示"拖拽组件到此处开始设计"）
- 右侧：属性面板（显示"请选择一个组件"）

### 步骤 2: 创建简单模板

1. **添加标题**
   - 从左侧拖拽"标题"到画布
   - 点击标题组件
   - 在右侧属性面板：
     * 标题内容：输入"订单合同"
     * 对齐方式：选择"居中"
     * 颜色：输入 #1890ff

2. **添加订单信息**
   - 拖拽"订单信息"到画布
   - 点击组件，修改标题为"订单详情"

3. **添加商品表格**
   - 拖拽"商品表格"到画布
   - 默认配置即可

4. **添加总金额**
   - 拖拽"总金额"到画布
   - 点击组件，勾选"显示原价"和"显示优惠"

### 步骤 3: 保存模板

1. 点击顶部"保存模板"按钮
2. 等待保存成功提示

**预期**: 
- ✅ 显示"模板保存成功"消息
- ✅ 数据库 `Setting` 表中应有 `order_contract_template_json` 记录

### 步骤 4: 验证模板加载

1. 刷新页面 (F5)
2. 检查画布中的组件是否还在

**预期**: 
- ✅ 所有组件保持原样
- ✅ 属性没有丢失

### 步骤 5: 下载合同测试

1. 访问客户端订单列表
   ```
   http://localhost:3000/orders
   ```

2. 点击任一订单进入详情页

3. 点击"下载合同"按钮

**预期**: 
- ✅ PDF 文件开始下载
- ✅ 文件名格式：`合同-订单号-日期.pdf`
- ✅ 打开 PDF，看到自定义模板的内容
- ✅ 中文字符正确显示
- ✅ 表格、金额格式正确

## 完整功能测试

### 测试所有组件类型

创建一个包含所有 11 种组件的模板：

```
1. 标题 (H1) - "订单合同"
2. 文本 - 公司信息
3. 分隔线 - 蓝色
4. 订单信息
5. 客户信息
6. 地址信息
7. 间距 (30px)
8. 商品表格
9. 总金额
10. 间距 (40px)
11. 签名栏
```

### 测试属性编辑

每个组件都点击并编辑其属性：

- **文本**: 修改字体大小、颜色、加粗
- **标题**: 修改级别、对齐方式
- **图片**: 输入测试图片 URL（如：`https://via.placeholder.com/150`）
- **分隔线**: 修改颜色、粗细
- **间距**: 修改高度
- **表格**: 修改表头颜色、勾选显示序号

### 测试拖拽排序

1. 拖动组件上下移动
2. 验证顺序改变
3. 保存后刷新，验证顺序保持

### 测试删除功能

1. 点击组件的删除按钮（红色垃圾桶图标）
2. 验证组件被移除
3. 验证属性面板恢复到"请选择一个组件"

### 测试重置功能

1. 修改几个组件
2. 点击顶部"重置"按钮
3. 确认对话框
4. 验证模板恢复为 DEFAULT_TEMPLATE（13个组件）

## 数据库验证

### 查询保存的模板

```sql
SELECT key, value 
FROM "Setting" 
WHERE key = 'order_contract_template_json';
```

**预期**: 
- 返回 JSON 字符串
- 包含 `components` 数组
- 包含 `styles` 对象

### 查看模板结构

```bash
# 在浏览器控制台执行
fetch('/api/admin/settings', {
  headers: { 'X-Auth-Type': 'admin' }
})
  .then(r => r.json())
  .then(d => {
    const tpl = d.settings.find(s => s.key === 'order_contract_template_json')
    console.log(JSON.parse(tpl.value))
  })
```

## 常见问题排查

### 问题 1: 拖拽不工作

**症状**: 组件无法拖动

**检查**:
1. 浏览器控制台是否有错误
2. @dnd-kit 包是否正确安装
3. 尝试刷新页面

### 问题 2: 保存失败

**症状**: 点击保存后提示失败

**检查**:
1. 管理员是否有 `system.manage` 权限
2. 数据库连接是否正常
3. 查看服务器日志
4. 检查网络请求（F12 → Network）

### 问题 3: PDF 不包含自定义模板

**症状**: 下载的 PDF 使用的是默认模板

**检查**:
1. 模板是否保存成功
2. 刷新订单页面再试
3. 检查 API 日志：
   ```
   读取合同模板失败: ...
   ```
4. 验证数据库中的 JSON 格式是否正确

### 问题 4: 中文显示为方框

**症状**: PDF 中中文显示为 ▯▯▯

**检查**:
1. 确认使用的是 jsPDF.html() 方法（不是直接 jsPDF.text()）
2. 查看 `DownloadContractButton.tsx` 代码
3. 浏览器是否支持（推荐 Chrome）

### 问题 5: 组件属性不生效

**症状**: 修改属性后，下载的 PDF 没有变化

**检查**:
1. 是否点击了"保存模板"
2. 刷新页面验证属性是否保存
3. 查看 `renderComponent()` 函数是否正确处理该属性

## 性能测试

### 大量组件测试

1. 添加 50+ 个组件
2. 测试拖拽性能
3. 测试保存速度
4. 测试 PDF 生成时间

### 大订单测试

1. 创建包含 100+ 个商品的订单
2. 下载合同
3. 测试表格渲染
4. 测试 PDF 文件大小

## 下一步

测试通过后，考虑：

1. **添加预览功能**: 在保存前预览模板效果
2. **多模板支持**: 为不同产品类别创建不同模板
3. **模板市场**: 提供预制模板供选择
4. **导出功能**: 导出模板 JSON 文件备份

## 反馈

测试过程中发现的问题或建议：

- [ ] 功能问题
- [ ] UI/UX 改进建议
- [ ] 性能问题
- [ ] 新功能需求

---

**测试时间**: ________  
**测试人**: ________  
**测试结果**: ☐ 通过 ☐ 失败  
**备注**: _______________
