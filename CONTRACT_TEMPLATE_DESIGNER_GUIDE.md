# 合同模板可视化设计器使用指南

## 功能概述

这是一个拖拽式的订单合同模板设计器，允许管理员自定义合同布局和样式，无需编写代码。

## 核心功能

### 1. 组件库（左侧面板）

包含 11 种可拖拽组件：

#### 基础组件
- **文本** - 普通段落文本
- **标题** - H1-H6 标题
- **图片** - 插入图片
- **分隔线** - 水平分割线
- **间距** - 空白间距

#### 数据组件
- **订单信息** - 订单号、创建时间、状态等
- **客户信息** - 客户姓名、邮箱、电话
- **地址信息** - 收货地址详情
- **商品表格** - 商品列表明细表
- **总金额** - 订单总金额汇总
- **签名栏** - 签名区域

### 2. 模板画布（中间区域）

- 从左侧拖拽组件到画布
- 点击组件进行选择
- 拖动组件重新排序
- 删除不需要的组件

### 3. 属性面板（右侧面板）

选中组件后，可编辑其属性：

#### 文本组件属性
- 内容
- 字体大小
- 对齐方式（左/中/右）
- 颜色
- 加粗/斜体

#### 标题组件属性
- 标题内容
- 级别（H1-H6）
- 对齐方式
- 颜色

#### 图片组件属性
- 图片地址
- 替代文字
- 宽度/高度
- 对齐方式

#### 订单信息组件
- 标题文字
- 自动包含字段：订单号、创建时间、订单状态、支付状态

#### 商品表格组件
- 标题文字
- 表头颜色
- 是否显示序号
- 默认列：SKU、商品名称、数量、单价、小计

#### 总金额组件
- 对齐方式
- 是否显示原价
- 是否显示优惠

#### 签名栏组件
- 标签文字
- 签名区宽度
- 是否显示日期

## 使用流程

### 第一步：设计模板

1. 访问 `/admin/contract-template`
2. 从左侧组件库拖拽组件到画布
3. 点击组件，在右侧属性面板编辑
4. 调整组件顺序（拖动重排）
5. 删除不需要的组件

### 第二步：保存模板

1. 点击顶部工具栏的"保存模板"按钮
2. 模板以 JSON 格式保存到数据库（`order_contract_template_json` 配置项）

### 第三步：预览模板

1. 点击"预览"按钮
2. 查看使用模拟数据渲染的合同效果
3. 检查所有组件是否正确显示
4. 验证样式和布局

### 第四步：打印预览

**方式一：直接打印**
1. 点击顶部工具栏的"打印"按钮
2. 系统自动生成预览并打开打印对话框
3. 可选择打印机或保存为 PDF

**方式二：先预览后打印**
1. 点击"预览"按钮查看效果
2. 在预览窗口点击"打印预览"按钮
3. 打开打印对话框进行打印

**打印特性：**
- ✅ A4 纸张尺寸
- ✅ 自动分页处理
- ✅ 保留颜色和样式
- ✅ 表格智能分页
- ✅ 避免内容截断

### 第五步：客户下载合同

1. 客户在订单详情页点击"下载合同"
2. 系统自动使用设计的模板渲染订单数据
3. 生成 PDF 文件供下载

## 技术实现

### 组件类型定义

所有组件类型定义在 `types/contract-template.ts`：

```typescript
export type TemplateComponentType = 
  | 'text' 
  | 'heading' 
  | 'image' 
  | 'divider' 
  | 'spacer'
  | 'orderInfo' 
  | 'customerInfo' 
  | 'addressInfo' 
  | 'productTable' 
  | 'totalAmount' 
  | 'signature'
```

### 模板渲染流程

1. **设计阶段**：管理员在 `/admin/contract-template` 设计模板
2. **保存阶段**：模板 JSON 保存到 `Setting` 表的 `order_contract_template_json` 键
3. **下载阶段**：
   - 客户请求下载合同
   - API 从数据库加载模板 JSON
   - `renderContractTemplate()` 将模板 + 订单数据渲染成 HTML
   - 前端使用 jsPDF.html() 将 HTML 转换为 PDF

### 文件结构

```
types/
  contract-template.ts              # 组件类型定义

lib/
  contract-template-renderer.ts     # 模板渲染器

components/contract-template/
  ComponentPalette.tsx              # 组件库面板
  TemplateCanvas.tsx                # 画布区域
  PropertiesPanel.tsx               # 属性编辑面板

app/admin/contract-template/
  page.tsx                          # 设计器主页面

app/api/client/orders/[id]/contract/
  route.ts                          # 客户合同下载 API

app/api/admin/orders/[id]/contract/
  route.ts                          # 管理员合同下载 API
```

## 数据流

### 模板保存

```
设计器页面 
  → POST /api/admin/settings 
  → 保存 { key: 'order_contract_template_json', value: JSON.stringify(template) }
```

### 合同下载

```
客户点击下载
  → GET /api/client/orders/[id]/contract
  → 读取 order_contract_template_json
  → renderContractTemplate(template, orderData)
  → 返回 HTML
  → 前端 jsPDF.html() 生成 PDF
```

## 默认模板

系统提供了一个完整的默认模板 `DEFAULT_TEMPLATE`，包含：

1. 标题："订单合同"
2. 公司名称文本
3. 蓝色分隔线
4. 订单信息（4个字段）
5. 客户信息（3个字段）
6. 收货地址
7. 商品明细表（5列）
8. 总金额汇总
9. 间距
10. 签名栏
11. 底部法律声明

## 扩展功能（未来）

- [x] ~~模板预览使用模拟数据~~ ✅ 已实现
- [x] ~~打印预览功能~~ ✅ 已实现
- [ ] 模板版本管理
- [ ] 多模板支持（不同订单类型使用不同模板）
- [ ] 导出/导入模板 JSON
- [ ] 更多组件类型（二维码、条形码、条件显示）
- [ ] 全局样式设置（页面大小、边距、字体）
- [ ] 拖拽调整组件大小和位置（网格布局）

## 常见问题

### Q: 如何重置为默认模板？
A: 点击顶部工具栏的"重置"按钮，确认后恢复默认模板。

### Q: 保存的模板在哪里？
A: 保存在数据库的 `Setting` 表，键名为 `order_contract_template_json`。

### Q: 客户能看到模板设计器吗？
A: 不能，设计器只对管理员开放（`/admin/contract-template`）。

### Q: 修改模板后，旧订单的合同会改变吗？
A: 会的，合同是实时渲染的，修改模板后所有合同都使用新模板。

### Q: 如何添加公司 Logo？
A: 拖拽"图片"组件到画布，在属性面板输入 Logo 的 URL 地址。

### Q: 中文显示有问题吗？
A: 不会，系统使用 HTML 渲染，完美支持中文字符。

## 注意事项

1. **保存后生效**：设计完模板后必须点击"保存"按钮，否则客户下载时使用旧模板
2. **字段名称固定**：数据组件（订单信息、客户信息等）的字段是预定义的，不可自定义
3. **图片地址**：使用图片组件时，确保图片 URL 可公开访问
4. **浏览器兼容**：建议使用 Chrome 或 Edge 浏览器进行设计
5. **数据安全**：模板中不要硬编码敏感信息，所有数据应来自订单记录

## 技术支持

如需帮助或功能建议，请联系技术团队。
