# 新功能实现说明

## 📦 发货与收货流程

### 1. 发票管理功能

#### 数据库模型
- **Invoice** 模型：
  - 发票类型（个人/企业）
  - 发票抬头
  - 税号（企业必填）
  - 接收邮箱
  - 开票状态（pending/processing/completed/rejected）
  - 发票号码、发票文件URL

#### API 接口
- **Admin 端**:
  - `GET /api/admin/invoices` - 获取所有发票列表
  - `GET /api/admin/invoices/[id]` - 获取发票详情
  - `PATCH /api/admin/invoices/[id]` - 更新发票状态/信息

- **Client 端**:
  - `GET /api/client/invoices` - 获取用户的发票列表

#### 使用流程
1. 用户下单时可选择开具发票
2. 填写发票信息（类型、抬头、税号等）
3. 管理员在后台处理开票请求
4. 更新发票状态并上传发票文件
5. 用户可查看和下载发票

---

### 2. 物流公司管理功能

#### 数据库模型
- **CourierCompany** 模型：
  - 公司名称（如：顺丰速运）
  - 公司代码（如：SF）
  - 官网、客服电话
  - 排序、状态（启用/停用）

#### 已添加的物流公司
1. 顺丰速运 (SF)
2. 中通快递 (ZTO)
3. 圆通速递 (YTO)
4. 申通快递 (STO)
5. 韵达快递 (YD)
6. 京东物流 (JD)
7. 邮政EMS (EMS)

#### API 接口
- **Admin 端**:
  - `GET /api/admin/courier-companies` - 获取所有物流公司
  - `POST /api/admin/courier-companies` - 创建物流公司
  - `GET /api/admin/courier-companies/[id]` - 获取单个物流公司
  - `PATCH /api/admin/courier-companies/[id]` - 更新物流公司
  - `DELETE /api/admin/courier-companies/[id]` - 删除物流公司

- **Client 端**:
  - `GET /api/client/courier-companies` - 获取启用的物流公司列表

#### 发货流程
1. 管理员进入订单详情页面
2. 订单状态为"已支付"时，显示"立即发货"按钮
3. 点击发货，弹出物流信息表单
4. **从下拉列表选择物流公司**（不再是手动输入）
5. 填写快递单号和备注
6. 确认发货，订单状态变为"已发货"

---

### 3. 用户确认收货功能

#### API 接口
- `POST /api/client/orders/[id]/confirm-delivery` - 确认收货

#### 触发流程
1. 订单状态为 `shipped`（已发货）时
2. 用户在订单详情页看到**"确认收货"按钮**
3. 点击按钮，弹出确认对话框
4. 用户确认后，订单状态变为 `delivered`（已送达）
5. 交易完成

#### 页面展示
- **订单列表页**: 显示订单状态
- **订单详情页**: 
  - 显示订单进度条（提交订单 → 支付完成 → 商品发货 → 确认收货）
  - 已发货状态：显示"确认收货"按钮
  - 已送达状态：显示"交易完成"标签
  - 显示物流信息

---

### 4. Token 隔离修复

#### 问题
之前 admin 和 client 共用同一个 token，导致在 admin 退出登录后，切换到 client 仍然是登录状态。

#### 解决方案
1. **分离 Cookie 存储**:
   - Admin 使用: `admin_token`
   - Client 使用: `client_token`

2. **修改文件**:
   - `contexts/AuthContext.tsx` - 根据路径选择对应的 token
   - `lib/api-utils.ts` - 根据 API 路径读取对应的 token

3. **实现细节**:
   - 检测当前路径是否以 `/admin` 开头
   - 登录时保存到对应的 cookie
   - API 请求时读取对应的 token
   - 退出登录时清除对应的 token

---

## 📊 订单状态流转

```
pending (待支付) 
    ↓ [用户支付]
paid (已支付)
    ↓ [管理员发货]
shipped (已发货)
    ↓ [用户确认收货]
delivered (已送达) ✅
```

---

## 🎯 使用指南

### 管理员操作
1. **处理订单**:
   - 查看订单详情
   - 选择物流公司（下拉选择）
   - 填写快递单号
   - 点击发货

2. **管理物流公司**:
   - 添加新的物流公司
   - 编辑现有公司信息
   - 启用/停用物流公司

3. **处理发票**:
   - 查看用户发票申请
   - 更新开票状态
   - 上传发票文件

### 用户操作
1. **下单时开发票**:
   - 选择是否需要发票
   - 填写发票信息

2. **确认收货**:
   - 订单状态为"已发货"时
   - 点击"确认收货"按钮
   - 确认对话框中再次确认

3. **查看物流**:
   - 订单详情页查看物流信息
   - 显示物流公司和快递单号

---

## ⚠️ 注意事项

1. **确认收货不可撤销**: 用户确认收货后，订单将标记为已完成
2. **物流公司管理**: 删除物流公司前需确保没有订单使用该公司
3. **Token 隔离**: 建议清除浏览器缓存后重新登录以应用新的 token 机制
4. **发票状态**: 管理员需要及时处理用户的发票申请

---

## 🚀 下一步建议

1. 添加物流追踪功能（对接第三方物流API）
2. 自动发送确认收货提醒（发货7天后）
3. 发票电子化下载
4. 订单评价功能
5. 售后退货流程优化
