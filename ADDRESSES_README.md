# 收货地址管理功能说明

## 功能概述

全新的收货地址管理页面提供了现代化的用户体验，包括地址的添加、编辑、删除和设置默认地址等功能。

## 主要特性

### 🎨 现代化UI设计
- 响应式设计，支持桌面端和移动端
- 卡片式布局，直观的地址展示
- 橙色主题色彩，与品牌风格保持一致

### 📍 智能地址选择器
- 省市二级联动选择
- 支持搜索功能，快速定位
- 数据验证，确保地址信息的准确性

### ✅ 表单验证
- 实时表单验证
- 手机号格式验证
- 必填字段检查
- 邮编格式验证

### 🏷️ 默认地址管理
- 一键设置默认地址
- 默认地址特殊标识显示
- 智能排序（默认地址优先）

### 📱 移动端优化
- 触控友好的按钮设计
- 自适应网格布局
- 优化的表单输入体验

## 使用说明

### 添加新地址
1. 点击"添加新地址"按钮
2. 填写收货人信息（姓名、电话）
3. 选择省市信息
4. 输入详细地址和邮编
5. 可选择设为默认地址
6. 点击"保存地址"

### 编辑地址
1. 在地址卡片上点击"编辑"按钮
2. 修改需要更新的信息
3. 保存更改

### 设置默认地址
- 在地址卡片上点击"设为默认"
- 或在编辑时勾选"设为默认地址"

### 删除地址
1. 点击地址卡片上的删除按钮
2. 确认删除操作

## 技术实现

### 前端技术栈
- Next.js 14 (App Router)
- React Hooks (useState, useEffect)
- Ant Design 组件库
- Tailwind CSS 样式
- TypeScript 类型安全

### 主要组件
- `AddressForm`: 地址表单组件，支持省市区联动
- `AddressCard`: 地址卡片组件，展示地址信息
- `AddressSelector`: 地址选择器组件（集成在表单中）

### API接口
- `GET /api/client/addresses`: 获取用户地址列表
- `POST /api/client/addresses`: 创建新地址
- `PATCH /api/client/addresses/[id]`: 更新地址
- `DELETE /api/client/addresses/[id]`: 删除地址

### 数据验证
- 前端表单验证
- 后端API验证
- 手机号正则表达式验证
- 邮编格式验证

## 地址数据结构

```typescript
interface Address {
  id: string
  userId: string
  name: string          // 收货人姓名
  phone: string         // 联系电话
  province: string      // 省份
  city: string          // 城市
  district?: string     // 区县（可选）
  detail: string        // 详细地址
  postalCode?: string   // 邮政编码
  isDefault: boolean    // 是否默认地址
  createdAt: Date
  updatedAt: Date
}
```

## 未来优化计划

1. **地图定位**: 集成地图服务，支持地址定位和验证
2. **地址模板**: 提供常用地址模板，快速填写
3. **地址导入**: 支持从其他平台导入地址
4. **地址标签**: 为地址添加标签（如家、公司等）
5. **智能推荐**: 基于历史订单推荐地址

## 注意事项

- 默认地址在结算时会自动选中
- 删除地址前会进行确认提示
- 表单提交时会进行完整的验证
- 地址信息会实时保存到数据库
