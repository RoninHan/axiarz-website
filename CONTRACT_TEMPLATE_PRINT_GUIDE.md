# 合同模板设计器 - 打印预览功能说明

## 新增功能

### 1. 实时预览

**位置**: 顶部工具栏 "预览" 按钮

**功能**:
- 使用真实的模板渲染引擎
- 显示模拟订单数据的渲染效果
- 900px 宽的模态窗口
- 支持滚动查看长内容

**模拟数据包含**:
- 订单号: ORD-2024-001
- 客户信息: 张三, zhangsan@example.com, 13800138000
- 地址: 广东省深圳市南山区科技园
- 2 个测试商品
- 原价 1500 元，优惠 303 元，实付 1197 元

### 2. 打印预览

**方式一: 快速打印**
```
点击顶部 "打印" 按钮 → 自动预览 → 打开打印对话框
```

**方式二: 先预览后打印**
```
点击 "预览" → 查看效果 → 点击 "打印预览" 按钮
```

**打印特性**:

1. **标准 A4 纸张**
   - 自动设置为 A4 尺寸
   - 适配打印机和 PDF 导出

2. **智能分页**
   - 表格自动分页
   - 避免内容在页面边界截断
   - 表头在每页重复显示

3. **颜色保留**
   - 使用 `-webkit-print-color-adjust: exact`
   - 确保背景色和边框颜色正确打印

4. **响应式布局**
   - 最大宽度 210mm (A4 标准宽度)
   - 屏幕预览时居中显示
   - 打印时自动适配

### 3. 技术实现

**新增文件**:
- `lib/print-utils.ts` - 打印样式和 HTML 包装工具

**核心函数**:

```typescript
// 包装 HTML 并添加打印样式
wrapHtmlForPrint(html: string, title: string): string

// 生成打印专用 CSS
getPrintStyles(): string
```

**打印流程**:

```
设计模板 
  → 点击预览/打印 
  → renderContractTemplate(template, mockData) 
  → wrapHtmlForPrint(html)
  → window.open() 
  → print()
```

## 使用示例

### 测试打印功能

1. **设计一个简单模板**
   ```
   - 标题: "测试合同"
   - 订单信息组件
   - 商品表格组件
   - 总金额组件
   ```

2. **预览效果**
   - 点击 "预览" 按钮
   - 检查所有组件是否正确渲染
   - 滚动查看完整内容

3. **打印测试**
   - 点击预览窗口的 "打印预览"
   - 在打印对话框中选择:
     * 目标: "另存为 PDF" (测试)
     * 或选择实际打印机
   - 验证 PDF/打印输出质量

### 打印优化建议

1. **合理使用分隔线**
   - 使用分隔线组件分隔不同区域
   - 避免内容过于拥挤

2. **控制表格行数**
   - 大量商品时表格会自动分页
   - 每页显示完整的表头

3. **注意颜色选择**
   - 深色背景在打印时消耗墨水
   - 建议使用浅色或白色背景

4. **字体大小**
   - 正文建议 12px 以上
   - 标题建议 16-24px
   - 确保打印清晰可读

## 打印样式详解

### 媒体查询

```css
@media print {
  /* 打印时的样式 */
  @page {
    size: A4;
    margin: 0;
  }
}

@media screen {
  /* 屏幕预览样式 */
  body {
    background-color: #525659;
  }
}
```

### 分页控制

```css
/* 在元素前强制分页 */
.page-break-before {
  page-break-before: always;
}

/* 在元素后强制分页 */
.page-break-after {
  page-break-after: always;
}

/* 避免元素内部分页 */
.avoid-break {
  page-break-inside: avoid;
}
```

### 表格打印优化

```css
table {
  page-break-inside: auto;  /* 允许表格跨页 */
}

tr {
  page-break-inside: avoid;  /* 避免行中间分页 */
}

thead {
  display: table-header-group;  /* 每页重复表头 */
}
```

## 常见问题

### Q: 打印时颜色丢失？
A: 在打印对话框中勾选 "背景图形" 或 "打印背景颜色"

### Q: 内容被截断？
A: 检查内容是否超出 A4 页面高度，系统会自动分页

### Q: 打印窗口被阻止？
A: 检查浏览器弹窗设置，允许当前网站打开弹窗

### Q: 打印预览显示空白？
A: 等待 250ms 让内容加载完成，或刷新打印窗口

### Q: 想导出 PDF 而不是打印？
A: 在打印对话框中选择 "另存为 PDF" 或 "Microsoft Print to PDF"

## 浏览器兼容性

| 浏览器 | 打印预览 | 颜色保留 | 分页控制 |
|--------|---------|---------|---------|
| Chrome | ✅ | ✅ | ✅ |
| Edge | ✅ | ✅ | ✅ |
| Firefox | ✅ | ✅ | ✅ |
| Safari | ✅ | ⚠️ 需设置 | ✅ |

**建议**: 使用 Chrome 或 Edge 浏览器获得最佳打印效果

## 更新日志

**2026-01-05**
- ✅ 添加真实数据预览功能
- ✅ 添加打印预览功能
- ✅ 创建打印样式工具 (`lib/print-utils.ts`)
- ✅ 优化预览模态框样式
- ✅ 添加快速打印按钮

---

**文档版本**: 1.1  
**最后更新**: 2026年1月5日
